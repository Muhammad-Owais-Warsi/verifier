Build a system using trigger dev that synchronize github repositories(several) for many customers.
the customers can sync the manually also. there should no conflicts in syncing and if there any resolve them fairly.
there should be no issues in processing or lags or system failiurs. customer shouldnt be consuming all the server capacity,
